// กฎ/ค่าคงที่ของการออกบิลและสัญญา (แก้ค่าที่นี่เพื่อปรับนโยบายหอพัก)
// อ้างอิงกฎธุรกิจใน docs/M10_DESIGN.md
module.exports = {
    // ค่าปรับจ่ายบิลช้า (รายเดือน) — บาท/วันที่เกินกำหนด · admin แก้รายบิลได้
    LATE_FEE_PER_DAY: 50,

    // ค่าปรับเช็คเอาท์เกินเวลา (รายวัน) — บาท/ชั่วโมงที่เกินเวลากำหนด
    LATE_CHECKOUT_PER_HOUR: 50,

    // ค่ามัดจำกุญแจเริ่มต้น (ใช้ตอนทำสัญญาถ้าไม่ได้ระบุ) — บาท
    DEFAULT_KEY_DEPOSIT: 200,

    // ระยะสัญญาเริ่มต้น — เดือน (ยืดหยุ่นได้ ปรับตอนทำสัญญา)
    DEFAULT_CONTRACT_MONTHS: 12,

    // ฐานจำนวนวันสำหรับคิดค่าเช่ารายวัน (proration) — ค่าเช่า/วัน = price_monthly / 30
    PRORATION_DAYS: 30,

    // ระยะเวลาแจ้งย้ายออกล่วงหน้า — วัน (มีผลต่อการคืนค่าเช่าล่วงหน้าส่วนเกิน)
    NOTICE_DAYS: 30,
};
